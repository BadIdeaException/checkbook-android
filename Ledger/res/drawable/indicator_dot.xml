<?xml version="1.0" encoding="utf-8"?>

<!-- 
Slightly annoying, but doing this through a color state list doesn't work. 
Hence the redundant definitions
-->

<selector xmlns:android="http://schemas.android.com/apk/res/android">

	<item android:state_selected="true">
    	<shape android:shape="oval">
            <size android:height="4dp" android:width="4dp" />

            <padding android:bottom="2dp" android:left="2dp" android:right="2dp" android:top="2dp" />

            <solid android:color="@android:color/holo_blue_bright" /> <!-- Unfortunately, can't use android:attr here -->
        </shape>
	</item>
    
    <item>
		<shape android:shape="oval">
            <size android:height="4dp" android:width="4dp" />

            <padding android:bottom="2dp" android:left="2dp" android:right="2dp" android:top="2dp" />

            <solid android:color="@android:color/darker_gray" />
        </shape>
	</item>

</selector>